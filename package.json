{
  "name": "nicepay-payment-gateway",
  "version": "2.0.1",
  "private": true,
  "license": "GPL-2.0-or-later",
  "description": "JavaScript quality gates for the NicePay WordPress plugin",
  "scripts": {
    "lint": "eslint assets/js .github/scripts tests/js eslint.config.js",
    "lint:css": "node .github/scripts/check-css.js",
    "test": "node --test tests/js/*.test.js",
    "quality": "npm run lint && npm run lint:css && npm test"
  },
  "devDependencies": {
    "css-tree": "^3.1.0",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "jquery": "^3.7.1",
    "jsdom": "^26.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
